// This file collects shared TypeScript types and interfaces for the project.
// It belongs to the global types module, which can be imported from various features.

// ============================
// Imports
// ============================

// There are no external imports yet; add them here when you introduce new shared types.

// ============================
// Main logic (type definitions)
// ============================

/**
 * Basic example type for an item that might be displayed in the UI.
 * Extend or replace this with real domain models as you build features.
 */
export type SimpleItem = {
  id: string;
  label: string;
};

